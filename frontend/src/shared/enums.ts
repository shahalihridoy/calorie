export enum AuthRoles {
  ADMIN = "admin",
  USER = "user",
}

export enum Languages {
  ENGLISH = "en-US",
  HEBREW = "he-IL",
  ARABIC = "ar-UA",
}

export enum OrderStatus {
  PENDING = "pending",
  ACCEPTED = "accepted",
  REJECTED = "cancelled",
}
